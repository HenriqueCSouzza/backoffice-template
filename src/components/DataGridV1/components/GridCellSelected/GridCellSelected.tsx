import { Checkbox } from "@/components/ui/checkbox";

export default function GridCellSelected({}) {
  return (
    <div className="px-4 flex items-center justify-center ">
      <Checkbox />
    </div>
  );
}
